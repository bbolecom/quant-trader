[pytest]
testpaths = tests
pythonpath = .
python_files = test_*.py
python_functions = test_*
addopts = -q
markers =
    network: 需要真实联网（CI 用 -m "not network" 跳过，避免 flaky）
filterwarnings =
    ignore::DeprecationWarning
    ignore::FutureWarning
